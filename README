Build dependencies: flex (>= 2.3.35), bison, cmake, apr, and
apr-util. At the moment, the binaries provided by each of these
packages (flex, bison, apr-config, and apu-config) must be in the
$PATH.

To build:

mkdir build
cd build
cmake ..
make

To run:

cd build
./src/cli/coverlog foo.olg
