define(input, keys(0), {int8});
define(add_one, keys(0), {int8, int8});
define(sub_500, keys(0), {int8, int8});

input(0);
input(X+1) :- input(X), X < 20;
add_one(A, A + 1) :- input(A);
sub_500(A, A - 500) :- input(A);

\dump input
\dump add_one
\dump sub_500
