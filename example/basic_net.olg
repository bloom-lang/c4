define(foo, keys(0), {@string, string});
define(bar, keys(0), {@string, string});
define(baz, keys(0), {@string, string});
define(baz2, keys(0), {@string, string, string});
define(self, keys(0), {string});

self("tcp:W88256X9YJX.local:10001");

bar(D, X) :- foo(D, X), baz(D, X), baz2(E, X, K), D == "tcp:W88256X9YJX.local:10001", D == E;
